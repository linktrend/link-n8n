name: 'Test: Linting'

on:
  workflow_call:
    inputs:
      ref:
        description: GitHub ref to lint.
        required: false
        type: string
        default: ''
      nodeVersion:
        description: Version of node to use.
        required: false
        type: string
        default: 24.13.1

env:
  # ubuntu-latest has ~7GB RAM; 7168 heap OOMs the lint job on GitHub-hosted runners.
  NODE_OPTIONS: ${{ vars.RUNNER_PROVIDER == 'github' && '--max-old-space-size=4096' || '--max-old-space-size=7168' }}
  TURBO_CONCURRENCY: ${{ vars.RUNNER_PROVIDER == 'github' && '2' || '4' }}

jobs:
  lint:
    name: Lint
    runs-on: ${{ vars.RUNNER_PROVIDER == 'github' && 'ubuntu-latest' || 'blacksmith-4vcpu-ubuntu-2204' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ inputs.ref }}

      - name: Build and Test
        uses: ./.github/actions/setup-nodejs
        with:
          build-command: pnpm lint
          node-version: ${{ inputs.nodeVersion }}
